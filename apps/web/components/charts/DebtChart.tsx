'use client';
// 국가채무 궤적 차트 — 막대 + GDP 비율 라인
export default function DebtChart({ data }: { data?: any[] }) {
  return (
    <div className="w-full h-96 flex items-center justify-center text-gray-400">
      <p>DebtChart (Recharts) — 데이터 연결 후 활성화</p>
    </div>
  );
}
