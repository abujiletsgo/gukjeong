'use client';
// 히트맵 — 부처별 의심 점수
export default function HeatMap({ data }: { data?: any[] }) {
  return (
    <div className="w-full h-96 flex items-center justify-center text-gray-400">
      <p>HeatMap (D3.js) — 데이터 연결 후 활성화</p>
    </div>
  );
}
