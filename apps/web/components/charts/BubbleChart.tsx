'use client';
// 버블 차트 — 국제 비교 또는 정책 영향 시각화
export default function BubbleChart({ data }: { data?: any[] }) {
  return (
    <div className="w-full h-96 flex items-center justify-center text-gray-400">
      <p>BubbleChart (D3.js) — 데이터 연결 후 활성화</p>
    </div>
  );
}
