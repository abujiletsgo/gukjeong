'use client';
// 스택드 에리어 차트 — 연도별 분야 지출 추이
// Recharts 기반
export default function StackedArea({ data }: { data?: any[] }) {
  return (
    <div className="w-full h-96 flex items-center justify-center text-gray-400">
      <p>StackedArea (Recharts) — 데이터 연결 후 활성화</p>
    </div>
  );
}
