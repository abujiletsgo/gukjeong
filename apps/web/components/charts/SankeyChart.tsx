'use client';
// Sankey 다이어그램 — 세입 → 부처 → 사업 흐름
// D3.js 기반 구현 예정
export default function SankeyChart({ data }: { data?: any }) {
  return (
    <div className="w-full h-96 flex items-center justify-center text-gray-400">
      <p>Sankey 다이어그램 (D3.js) — 데이터 연결 후 활성화</p>
    </div>
  );
}
