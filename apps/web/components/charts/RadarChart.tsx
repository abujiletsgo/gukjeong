'use client';
// 레이더 차트 — 국회의원 종합 평가
export default function RadarChart({ data }: { data?: any }) {
  return (
    <div className="w-full h-80 flex items-center justify-center text-gray-400">
      <p>RadarChart (Recharts) — 데이터 연결 후 활성화</p>
    </div>
  );
}
